[tool.poetry]
name = "loan-default-prediction"
version = "0.1.0"
description = ""
authors = ["Reinvdb <rein.vdb@hotmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
